import React from "react";
import SignUpPresenter from "./SignUpPresenter";

const SignUpContainer = ({ setIsSignUp }) => {
  return <SignUpPresenter setIsSignUp={setIsSignUp} />;
};

export default SignUpContainer;
